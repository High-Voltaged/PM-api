package utils

const (
	USER_EXISTS           = "A user with this email already exists."
	USER_NOEXIST          = "A user with this email doesn't exist."
	INCORRECT_CREDENTIALS = "The specified credentials are incorrect."
)
